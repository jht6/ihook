module.exports = (task) => {
    // TODO: 执行普通型任务，使用execa.shellSync()执行command即可
    console.log('========= commonExecutor start ==========');
    console.log(task);
    console.log('========= commonExecutor   end ==========');
    return 0;
};
